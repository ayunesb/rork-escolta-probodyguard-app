// Legacy: moved to cleanup-backup because `lib/firebase.ts` is now the single source of truth.
// This file has been archived to avoid duplicate Firebase initializations.
export const _LEGACY_CONFIG_FIREBASE = true;
